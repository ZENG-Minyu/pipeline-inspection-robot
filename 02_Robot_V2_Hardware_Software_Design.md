# 管道检测机器人 V2 — 硬件与软件系统设计技术文档

> 用途：ZENG Minyu Individual Section 3.5 & 3.6 "Robot Hardware Design" & "Robot Software Design" 写作素材
> 状态：已完成（基于源代码全文阅读）
> 作者：ZENG Minyu | 代码最后更新：2026.04.22

---

## 一、系统架构总览

### 1.1 整体控制架构

```
┌──────────────────────────────────────────────────────────┐
│                     上位机 (Host PC)                      │
│  - python/serial_logger.py: 串口数据采集                  │
│  - python/log_to_coords.py: 步数→柱坐标转换               │
└─────────────────┬────────────────────────────────────────┘
                  │ USB Serial (115200 baud)
┌─────────────────▼────────────────────────────────────────┐
│              下位机主控 (Arduino Mega 2560)                │
│                                                          │
│  ┌──────────┐  ┌───────────┐  ┌────────────────────┐    │
│  │ PS2遥控  │  │ OLED显示  │  │ CNC Shield v3      │    │
│  │ 接收模块 │  │ SSD1306   │  │ (4× A4988 Driver)  │    │
│  │ (SPI)    │  │ (I2C)     │  │                    │    │
│  └──────────┘  └───────────┘  └──────┬──┬──┬──┬────┘    │
│                                      │  │  │  │         │
│                                   M0 M1 M2 M3            │
│                                   X  Y  Z  A             │
└─────────────────────────────────────┬──┬──┬──┬──────────┘
                                      │  │  │  │
         ┌────────────────────────────┘  │  │  └──────────┐
         ▼                               ▼  ▼              ▼
   径向伸缩电机                    旋转扫描电机  (预留)  驱动轮×3
   (独立丝杠)                    (小齿轮/内齿圈)        (1/8微步)
```

### 1.2 电机编号与功能映射

| 软件索引 | CNC Shield 轴 | 硬件功能 | 关键参数 |
|---|---|---|---|
| Motor 0 = X | Axis X (STEP=D2, DIR=D5) | 探头径向伸缩（扫描臂升降） | ARM_EXTEND_STEPS=1600（全行程） |
| Motor 1 = Y | Axis Y (STEP=D3, DIR=D6) | 360° 旋转扫描（旋转圆盘） | 2230 steps = 360°（内齿圈减速比换算） |
| Motor 2 = Z | Axis Z (STEP=D4, DIR=D7) | 预留（当前未使用） | — |
| Motor 3 = A | Axis A (STEP=D12, DIR=D13) | 整机前进/后退（3 轮同步驱动） | 1/8 微步，1600 steps/rev，轮径 70mm |

> **设计说明：** Motor A (index 3) 在 CNC Shield 上对应 A 轴，但实际上同时驱动后段 3 个 Nema 17 驱动电机（接线端并联）。由于三个轮子需要同步转动来维持直线行走，统一由一个 A4988 通道控制是最简洁的方案。代码中对 "motorA" 的所有操作即等效于对 3 个驱动轮的同时操作。

### 1.3 软件项目结构（PlatformIO）

```
FYDP Project Mega/
├── platformio.ini            # 项目配置：Atmel AVR, Arduino Mega 2560
├── include/
│   ├── Config.h              # 引脚定义（CNC Shield + PS2）
│   ├── StepperMotor.h        # 步进电机驱动类
│   ├── PS2Driver.h           # PS2 手柄驱动类
│   ├── Relay.h               # 继电器驱动类
│   ├── OledDisplay.h         # OLED 显示驱动类
│   ├── DualMPU6050.h         # 双 MPU6050 IMU 姿态采集（已弃用）
│   ├── RollController.h      # 旋转角度闭环控制（已弃用）
│   └── AutomationSequence.h  # 自动化检测序列控制器
├── src/
│   ├── main.cpp              # 主程序入口
│   ├── StepperMotor.cpp
│   ├── PS2Driver.cpp
│   ├── Relay.cpp
│   ├── OledDisplay.cpp
│   ├── DualMPU6050.cpp       # （已弃用）
│   ├── RollController.cpp    # （已弃用）
│   └── AutomationSequence.cpp
├── python/
│   ├── serial_logger.py      # 串口数据采集
│   └── log_to_coords.py      # 步数 → 柱坐标转换
├── lib/                      # 本地库（U8g2, I2Cdev, PS2X, MPU6050）
├── backup/                   # 前期测试代码（已归档，不再使用）
└── test/                     # 前期测试代码（已归档，不再使用）
```

**外部依赖：** TimerOne（唯一通过 platformio.ini 声明的库依赖，用于定时器中断）

**本地库（lib/）：**
- U8g2 — OLED 图形库（SSD1306 128x64 I2C）
- PS2X_lib — PS2 无线手柄通信协议
- I2Cdevlib-Core + MPU6050 — IMU 传感器驱动（已弃用但代码保留）

---

## 二、硬件系统详细设计

### 2.1 主控板：Arduino Mega 2560

- **芯片**：ATmega2560
- **主频**：16 MHz
- **框架**：Arduino（PlatformIO 编译链）
- **串口通信**：USB Serial @ 115200 baud（与上位机通信）
- **供电**：通过方口 USB-B 打印机线接入 5V 电池

### 2.2 电机驱动：CNC Shield v3 + A4988

**CNC Shield v3 引脚映射（定义于 Config.h）：**

```
PIN_EN    = D8    (Enable — 全部4轴共用使能，LOW = 使能)
X_STEP    = D2    X_DIR = D5
Y_STEP    = D3    Y_DIR = D6
Z_STEP    = D4    Z_DIR = D7
A_STEP    = D12   A_DIR = D13
```

**A4988 驱动配置：**
- 数量：4 块（对应 X/Y/Z/A 四轴）
- 逻辑电压：5V（由 12V→5V DC-DC 降压模块提供）
- 动力电压：12V（由独立电池 → 继电器控制供给）
- **A 轴微步设置**：通过 MS1/MS2/MS3 跳线配置为 **1/8 微步**
  - 200 步/转 × 8 = 1600 微步/转
  - 全步 1.8° → 微步分辨率 0.225°
- **其他轴（X/Y/Z）**：全步模式（未启用微步跳线）

> **代码验证：** `log_to_coords.py` 第 41 行定义 `STEPS_PER_MOTOR_REVOLUTION = 200 * 8 = 1600`，以及代码注释明确写到 "A电机使用 1/8 微步"。

### 2.3 电源系统

```
┌─────────────┐     USB-B 方口线     ┌──────────────────┐
│  5V 电池组   │ ──────────────────→  │  Arduino Mega    │
└─────────────┘                      │  (主板供电)      │
                                     └──────────────────┘

┌─────────────┐                      ┌──────────────────┐
│ 12V 电池组   │ ──→ [继电器 D52] ──→ │ A4988 ×4        │
└─────────────┘     (Active LOW)      │ (电机动力供电)   │
                                      └────────┬─────────┘
                                               │
                        ┌──────────────────────┘
                        ▼
                 ┌──────────────┐
                 │ 12V→5V DC-DC │ ──→ A4988 逻辑电源 (VDD)
                 │ 降压模块      │
                 └──────────────┘
```

**双电源隔离设计：**
- Arduino Mega 由独立 5V 电池供电（USB 口），与控制电路的逻辑电平匹配
- 12V 电池专供电机动力，通过继电器受控通断
- 12V→5V 降压模块从动力电池取电，为 A4988 的逻辑侧（VDD）供电，实现动力-逻辑隔离
- **安全机制**：继电器默认为断开（HIGH = OFF），上电时电机无动力，防止误动作

### 2.4 继电器控制

- **型号**：常开式低电平触发继电器
- **控制引脚**：D52
- **逻辑**：`LOW = ON`（通路，12V 送至 A4988），`HIGH = OFF`（断路）
- **初始化**：`begin()` 时先将引脚写 HIGH 再设为 OUTPUT，防止上电瞬时误触发
- **软件保护**：PS2 的 ▲/✕/R1/R2/□/UP 等所有电机操作均受 `relay.isOn()` 门控，继电器关闭时所有电机指令被拒绝并显示 "Access Denied"

### 2.5 PS2 无线遥控

- **通信协议**：SPI（Serial Peripheral Interface）
- **引脚定义**（Config.h）：

| 信号 | Arduino 引脚 |
|---|---|
| DAT (MISO) | D22 |
| CMD (MOSI) | D24 |
| SEL (CS)   | D26 |
| CLK (SCK)  | D28 |

- **库**：PS2X_lib（本地 lib 目录）
- **配置**：`config_gamepad(CLK, CMD, SEL, DAT, true, true)` — 启用压力感应和震动
- **读取循环**：每帧调用 `ps2.update()` → `ps2x.read_gamepad(false, 0)` → 轮询 16 个按键

### 2.6 OLED 显示屏

- **型号**：SSD1306 128×64 像素单色 OLED
- **通信**：I2C（硬件 I2C，SDA/SCL = D20/D21）
- **库**：U8g2（`U8G2_SSD1306_128X64_NONAME_1_HW_I2C`）
- **显示内容**：
  - 启动画面（Boot Screen）
  - 当前选中电机编号 + 实时步数
  - 电机运行状态
  - 继电器通断状态
  - 拒绝指令提示（继电器关闭时的 Access Denied 界面）

### 2.7 双 MPU6050 IMU 模块（已弃用）

> **设计初衷**：通过两个 MPU6050（I2C 地址 0x68 / 0x69）采集 Roll 角度，实现旋转圆盘的闭环角度控制。两个传感器取平均值可提高精度和冗余。
>
> **弃用原因**：
> 1. I2C 信号从前段经过长线缆（约 1m）传输到后段，信号衰减严重
> 2. 12V 电机 PWM 电路产生强电磁干扰，进一步恶化 I2C 通信稳定性
> 3. 传感器读数频繁失效，无法可靠用于闭环控制
>
> **替代方案**：改为**开环步数计数**——利用齿轮减速比和内齿圈传动关系，从步进电机步数直接推算旋转角度（2230 steps = 360°）
>
> **代码状态**：`DualMPU6050.cpp/h` 和 `RollController.cpp/h` 保留在项目中但 main.cpp 中未被引用，属于已归档代码。

---

## 三、软件系统详细设计

### 3.1 主程序流程（main.cpp）

```
setup():
  1. Serial.begin(115200)             // 初始化串口
  2. motorX/Y/Z/A.begin()             // 初始化四路电机引脚
  3. pinMode(PIN_EN, LOW)             // 使能 CNC Shield
  4. relay.begin()                    // 继电器初始化（默认断开）
  5. ps2.init()                       // PS2 手柄配对
  6. oled.begin() + showBootScreen()  // OLED 启动
  7. Timer1.initialize(100us)         // 配置 100μs 定时器中断
  8. Timer1.attachInterrupt(motorISR) // 绑定电机中断服务

loop():
  ps2.update()                  // [每帧] 读取手柄状态
  handlePS2Input()              // [每帧] 处理手柄按键
  oled.update()                 // [每帧] 刷新显示屏
  sequence.update()             // [每帧] 推进自动化序列
  检测序列状态变化 → 串口输出标记

定时器中断 (每 100μs):
  motorISR():
    motorX.updateDirectInInterrupt()
    motorY.updateDirectInInterrupt()
    motorZ.updateDirectInInterrupt()
    motorA.updateDirectInInterrupt()
```

> **中断 vs 主循环**：电机步进脉冲由 100μs 定时器中断驱动（`updateDirectInInterrupt`），保证了脉冲时序精度不受主循环频率波动影响。主循环仅负责按键解析、序列状态机和 OLED 刷新，不参与脉冲生成。

### 3.2 核心类设计

#### 3.2.1 StepperMotor（步进电机驱动）

这是整个软件系统的基石类，封装了所有步进电机操作。

**状态模型：**

| 模式 | 触发方式 | 停止条件 | 用途 |
|---|---|---|---|
| 绝对位置模式 | `moveTo(target, interval)` | 到达目标步数 | 曾经用于位置控制（当前主要用阻塞式） |
| 连续模式 | `run(dir, interval)` | 手动调用 `stop()` | PS2 ▲/✕ 持续运转 |
| 阻塞式步进 | `stepBlocking(steps, interval, callback)` | 走完指定步数 / callback 返回 false | 自动化序列 & PS2 R1/R2 点动 |

**关键实现细节：**
- 脉冲生成：`digitalWrite(STEP, HIGH)` → `delayMicroseconds(pulseWidth)` → `digitalWrite(STEP, LOW)`
- 脉冲宽度可配置（`setPulseWidth()`，默认 200μs），确保 A4988 有足够的上升沿检测时间
- `stepBlocking()` 禁用全局中断（`cli()`/`sei()`）以避免与 `motorISR` 定时器中断冲突
- 阻塞式步进支持 `shouldContinue` 回调参数，可在步进过程中检测外部条件中断（当前未使用，传入 nullptr）

**中断安全机制：**
两个更新函数各有用途：
- `update()` — 主循环调用（当前被注释，由中断替代）
- `updateDirectInInterrupt()` — 定时器中断 ISR 调用，直接生成脉冲，不依赖主循环调度

#### 3.2.2 PS2Driver（PS2 手柄驱动）

**16 键位掩码系统：**

```
索引: 0=SELECT, 1=L3, 2=R3, 3=START,
      4=↑, 5=→, 6=↓, 7=←,
      8=L2, 9=R2, 10=L1, 11=R1,
      12=▲, 13=○, 14=✕, 15=■
```

**三种按键检测模式：**
- `pressed(button)` — 当前帧按下（持续按住为 true）
- `justPressed(button)` — 下降沿检测（按下瞬间为 true，用于触发一次性动作）
- `justReleased(button)` — 上升沿检测（松开瞬间为 true）

**设计意义：** 通过区分 "按住" 和 "刚按下"，避免了按键重复触发。例如 ▲/✕ 持续运转用 `pressed()`，而 R1/R2 点动和 START 切换继电器用 `justPressed()`。

#### 3.2.3 Relay（继电器驱动）

- Active LOW 逻辑：`turnOn()` → `digitalWrite(pin, LOW)`；`turnOff()` → `digitalWrite(pin, HIGH)`
- 初始化防护：`begin()` 先写 HIGH 再设 OUTPUT，杜绝上电瞬间的 LOW 脉冲误触发
- `toggle()` 方法简化 PS2 START 按钮的开关切换逻辑

#### 3.2.4 OledDisplay（OLED 显示）

- 基于 U8g2 库的页缓冲模式（`_1_` = 第一页缓冲区，内存占用最低）
- `_dirty` 标志位 + `requestRefresh()` 机制避免不必要的屏幕刷新
- `showAccessDenied()` 实现带持续时间的警告界面（1.5 秒自动恢复）
- 显示分为正常状态（电机信息）和告警覆盖（Access Denied）两个层级

#### 3.2.5 AutomationSequence（自动化检测序列）

**序列步骤类型：**

```cpp
enum SequenceStepType {
    STEP_MOTOR_STEPS,   // 电机移动指定步数
    STEP_MOTOR_ANGLE,   // 按角度移动（预留，暂未实现）
    STEP_WAIT_MS,       // 等待毫秒
    STEP_MOTOR_EXTEND,  // 伸出摇臂（= +ARM_EXTEND_STEPS）
    STEP_MOTOR_RETRACT, // 收回摇臂（= -ARM_RETRACT_STEPS）
    STEP_REPEAT         // 重复序列（跳回第一步）
};
```

**默认检测序列（当前生效版本）：**

```
重复 5 次:
  1. Motor A (驱动轮): 前进 100 步
  2. Motor Y (旋转):   逆时针 30 步 × 4 = 120 步扫描
  3. Motor Y (旋转):   顺时针 120 步归位
```

**序列逻辑解读：**
- 每一步前进后，执行一段弧线扫描（120 步 Y 电机 = 约 19.4° 的扫描扇区）
- 扫描后旋转回归原位 → 再次前进 → 再次扫描
- 5 次循环共覆盖 5 个轴向位置 × 约 19.4° 的扫描范围
- 此序列为简化测试版本，完整版（注释区块中）曾包含摇臂伸出/收回 + 等待采集时间

**关键方法：**
- `start()` — 启动序列，重置到第一步
- `update()` — 主循环每帧调用，执行当前步骤并前进
- `executeOneStep()` — 单步执行（PS2 ↑ 触发），不依赖 `_running` 标志，与 `update()` 互不干扰
- `printMotorStates()` — 每步完成后向串口输出 4 电机当前步数

**序列触发方式：**
- PS2 ■ 键 → `setDefaultSequence()` + `start()` → 自动连续执行
- PS2 ↑ 键 → `executeOneStep()` → 手动单步执行（调试用）

### 3.3 PS2 手柄完整按键映射

| 按键 | 功能 | 触发模式 | 继电器保护 |
|---|---|---|---|
| L1 | 切换到上一个电机（索引+1） | justPressed | 否 |
| L2 | 切换到下一个电机（索引-1） | justPressed | 否 |
| R1 | 当前电机正向点动（步数因电机而异） | justPressed | ✅ |
| R2 | 当前电机反向点动 | justPressed | ✅ |
| ▲ | 当前电机连续正向运转 | pressed（按住） | ✅ |
| ✕ | 当前电机连续反向运转 | pressed（按住） | ✅ |
| ■ | 启动自动化检测序列 | justPressed | ✅ |
| ↑ | 单步执行序列（调试用） | justPressed | ✅ |
| ○ | 重置全部 4 个电机步数为 0 | justPressed | 否 |
| START | 切换继电器通断（12V 开关） | justPressed | 否 |
| SELECT | 串口打印当前 4 轴步数 | justPressed | 否 |

**R1/R2 点动步数（因选中电机不同而不同）：**

| 选中电机 | R1/R2 步数 | 物理意义 |
|---|---|---|
| Motor 0 (X) | ±800 | 摇臂半行程伸缩 |
| Motor 1 (Y) | ±25 | 旋转约 4° |
| Motor 2 (Z) | ±200 | 预留 |
| Motor 3 (A) | ±100 | 前进/后退约 13.7 mm |

### 3.4 定时器中断设计

```cpp
Timer1.initialize(100);  // 100 微秒周期 = 10 kHz 中断频率
Timer1.attachInterrupt(motorISR);
```

**为什么使用中断驱动电机？**
- 主循环包含 PS2 通信、OLED I2C 传输、串口输出等耗时操作，循环频率可能波动（实测可降至几十 Hz）
- 电机步进需要精确的微秒级脉冲间隔——例如 5500 μs 的步进间隔要求 ±100 μs 的精度才能避免抖动
- 100 μs 定时器中断提供比主循环高两个数量级的时间分辨率，确保脉冲时序精确
- 每个中断内顺序调用 4 个电机的 `updateDirectInInterrupt()`，按需生成脉冲

### 3.5 步进电机中频共振问题：诊断与解决

这是本项目软件开发过程中最具挑战性的技术问题，也是最终驱动方案的核心优化点。

#### 3.5.1 问题发现：从空转到带载的断裂

软件开发的初期版本使用主循环 `update()` 驱动电机（非中断方案）。测试过程分为三个阶段：

**阶段一（前段电机测试）**：Motor 0（径向）、Motor 1（旋转）、Motor 2（预留）空转或接丝杆时，虽然声音较大、有些抖动，但都能正常转动。此时问题尚未暴露。

**阶段二（整机联合测试）**：当机器人前后段组装完成，Motor A（驱动轮）装上橡胶轮后，发生了严重的抖振现象——电机原地剧烈抖动，"走两步退一步"，前进效率极低。更诡异的是：
- Motor A 空转（无任何负载）：正常转动（仅速度偏慢）
- Motor A 接上仅几十克的轻质橡胶轮（悬空、无地面接触）：剧烈抖振、回弹
- Motor A 接上丝杆等较重载荷（实际受力）：反而能正常缓慢转动

**阶段三（阻塞式同样失效）**：在阻塞模式 `stepBlocking()` 下将 speed 设为 700 μs（约 1.4 kHz），电机"剧烈抖动一下，一步都没有前进"。

#### 3.5.2 问题定位：主循环频率与电机共振的重合

**第一步诊断——测量主循环频率：**
在 `loop()` 中加入频率计数器（main.cpp 注释区块中的代码），测得主循环频率约为 **330 Hz**，即每个循环周期约 **3 ms**。

**这意味着什么？**
- 当代码设定 `speed = 700 μs` 时，期望脉冲间隔为 0.7 ms
- 但主循环每 3 ms 才执行一次 `motorX.update()`
- `update()` 逻辑每次只发 **一个脉冲**（`if` 而非 `while`）
- 实际脉冲间隔被强制拉伸到 3 ms 的整数倍
- 在 300–3000 μs 范围内调整 speed 参数**几乎不影响实际速度**——这就是"假的速度调节"

**第二步诊断——为什么轻的橡胶轮比重的丝杆更严重？**

在步进电机系统中，转子与负载构成一个扭转振荡系统：磁场提供恢复力矩（等效为扭转弹簧），转子加负载提供转动惯量。这一系统的固有频率随惯量增大而降低。三种工况的定性比较：

| 负载类型 | 现象 | 定性解释 |
|---|---|---|
| 电机空转 | 正常（仅速度偏慢） | 转子惯量最小，系统固有频率较高，远离 330 Hz 激励频率 |
| 轻质橡胶轮 | 剧烈抖振、回弹 | 橡胶轮增加了显著惯量，将系统固有频率拉低至 330 Hz 附近，进入共振区；同时橡胶几乎不提供阻尼，振荡持续累积 → 转子失步 |
| 丝杆重载 | 正常缓慢运行 | 丝杆惯量虽大但金属联轴器的刚性连接和丝杆本身的库仑摩擦提供了强机械阻尼，压制了振荡 |

**实验验证一——手握阻尼法：**

一个简单但决定性的实验确认了阻尼的关键角色：在电机带橡胶轮运行时，用手握住电机外壳或轮子。手的接触引入了大量阻尼，振动随即消失，电机恢复正常运转。这直接证明问题本质是**低阻尼下的共振**，而非电机本身或驱动器的缺陷。

**实验验证二——中断恢复后的频率确认：**

更进一步的验证出现在定时器中断部署之后。当脉冲时序已经精确到 100 μs 量级、波形不再杂乱时，若将中断模式下的步进间隔调至 3000–3500 μs（对应步进频率约 285–333 Hz），同样可以复现抖振。这说明问题的根源不仅是"波形杂乱"，更是**步进频率本身落入了带橡胶轮系统的共振区间**——无论脉冲是否均匀，只要频率对上了就会抖。

**第三步诊断——Loop 的"假调速"：**

上述现象揭示了旧代码架构的根本缺陷。Arduino Mega 在执行 PS2 通信、OLED 刷新和 I2C 传输等任务时，主循环周期被锁定在约 3 ms（~330 Hz）。`update()` 每次循环只发一个脉冲，导致：

- 无论代码中 `speed` 参数设为 700 μs 还是 3000 μs，实际脉冲间隔恒为 ~3 ms
- 用户无法选择避开共振区——因为循环频率被芯片性能和代码负载死死限制在 330 Hz 附近，恰好与带轮工况的共振频率重合
- 实现真正调速的唯一途径是将脉冲生成迁移到独立于主循环的硬件定时器

**第三步诊断——阻塞式为什么也失效？**

`stepBlocking(speed=700)` 对应约 1.4 kHz 的脉冲频率。42 步进电机在全步模式下，从静止直接启动到 1.4 kHz 需要极大的瞬时扭矩，超出了电机的**启动扭矩（Pull-in Torque）**极限。这就像汽车从 0 直接挂 5 档——熄火（失步）。将 speed 调至 3000 μs（约 333 Hz）后阻塞式恢复正常，验证了这一诊断。

#### 3.5.3 解决方案

**方案一（软件层）：Timer1 100 μs 定时器中断**

这是最核心的优化。将电机脉冲生成从主循环（精度 ±3 ms）迁移到硬件定时器中断（精度 ±100 μs）：

```
Timer1.initialize(100);  // 100 μs 周期 = 10 kHz 中断频率
Timer1.attachInterrupt(motorISR);
```

效果：
- 脉冲时序精度提升 **两个数量级以上**
- speed 参数从此真实生效——700 μs 和 3000 μs 的差异变得清晰可辨
- 连续模式（▲/✕ 按住）从"基本废的状态"变为平滑可控

**中断函数的设计要点：**
`updateDirectInInterrupt()` 采用异步状态机设计，而非简单的 `while` 追赶循环。每次中断仅做三件事：检查时间是否到了 → 拉高 STEP 引脚并标记 `_pulseActive` → 下一个中断检查 `_pulseWidthUs` 是否到期 → 拉低引脚。这种方式确保每次 ISR 执行时间在纳秒级，不影响主循环的 PS2 通信和 OLED 刷新。

**方案二（硬件层）：Motor A 单独启用 1/8 微步**

在全步模式下，电机每走一步都是"哐"地撞到下一个磁极位置，这种冲击本身就容易激发共振。1/8 微步将每一步拆成 8 个微小的台阶，电流在两相之间平滑过渡，从根本上消除了步进冲击。

关键决策——**混合微步策略**：
- Motor A（驱动轮）：**1/8 微步**。三个驱动轮直接接触管壁，轮-管界面的弹性接触最容易激发共振，必须细分。
- Motor 0/1/2（径向、旋转、预留）：**全步模式**。三者均通过丝杆或减速器传动，机械阻尼足够压制共振，全步模式力矩更大、控制更简单。
- 代码层面：微步倍率不在固件中处理，步数统一为"原始微步脉冲数"，由上位机 Python（`log_to_coords.py`）在坐标转换时除以对应倍率。这保持了固件的简洁性。

#### 3.5.4 验证与结论

经过上述两个方案叠加后：
- 所有电机的阻塞式运动正常，无论载荷大小均能稳定走完全程
- 连续模式（▲/✕）调速灵敏——speed 从 700 到 3000 速度差异明显
- Motor A 带橡胶轮的抖振和回弹彻底消失
- 即使不实现加速曲线（Ramp），在 speed = 3000 μs（333 Hz）以下也能直接起步

**结论：** 本问题的演化路径可归纳为三条工程经验：

1. **不要在低速 MCU 的主循环里做精确时序控制。** Loop 频率受代码负载制约，不可控也不稳定。任何需要微秒级精度的周期信号，必须交给硬件定时器中断。

2. **步进电机的负载不只是"重不重"的问题。** 一个几十克的橡胶轮比几公斤的丝杆更危险——因为后者提供了阻尼，前者只提供惯量。在惯量-阻尼参数空间中，低阻尼 + 中等惯量是最不利的组合。

3. **微步进是消除共振的硬件"万能药"。** 将全步拆成微步后，每一步的能量冲击大幅降低，即使频率仍在共振区附近，系统的响应幅度也远不足以导致失步。

---

## 四、Python 上位机数据处理管线

### 4.1 serial_logger.py — 串口数据采集

**功能：**
- 自动检测 Arduino 串口（匹配 `/dev/cu.usbmodem*` 或描述含 "Arduino" 的设备）
- 以 115200 baud 连接，每行读取并附带从首行开始的相对时间戳
- Ctrl+C 停止后自动保存为 `log_data_YYYY-MM-DDTHH-MM-SS.txt`

**输出格式示例：**
```
[+0.000] X: 0, Y: 0, Z: 0, A: 0
[+1.234] X: 0, Y: 30, Z: 0, A: 100
[+2.567] X: 0, Y: 60, Z: 0, A: 100
```

### 4.2 log_to_coords.py — 步数→柱坐标转换

**坐标映射关系：**

| 柱坐标分量 | 来源 | 换算公式 |
|---|---|---|
| r (径向) | 固定常值 | r = PIPE_RADIUS = 234.0 mm（管道内半径） |
| θ (角度) | Motor Y 步数 | θ_deg = steps_Y × 360° / 2230 |
| z (轴向) | Motor A 步数 | z = steps_A × π × WHEEL_RADIUS / 800 |

**z 轴换算推导：**
- A 电机：1/8 微步 → 1600 步/转
- `steps_A / 1600` = 电机转动圈数
- 每圈对应轮子行进 = π × 轮径 = π × 70 mm
- 每步行进 = π × 70 / 1600 = π × 35 / 800 mm
- `z = steps_A × π × WHEEL_RADIUS / 800 = steps_A × π × 35 / 800`

**θ 轴换算推导：**
- Y 电机通过小齿轮→内齿圈驱动旋转圆盘，总减速比使得 2230 steps = 360°
- 该数字从实际测试标定获得（非纯理论计算，因为包含了齿轮啮合间隙等因素）

**可调参数：**
```python
WHEEL_RADIUS = 35.0          # [mm] 轮子半径
PIPE_RADIUS = 234.0          # [mm] 管道内半径
STEPS_PER_Y_REVOLUTION = 2230 # Y 电机每圈所需步数
INPUT_FILE = "log_data_xxx.txt"  # 输入日志文件名
```

**输出 CSV 格式：**
```csv
elapsed_s,r_mm,theta_deg,theta_rad,z_mm,x_steps,y_steps,z_steps,a_steps
0.000,234.0000,0.0000,0.000000,0.0000,0,0,0,0
1.234,234.0000,4.8421,0.084518,13.7445,0,30,0,100
```

---

## 五、已知局限与未来改进方向

### 5.1 旋转角度开环控制

- **问题**：原计划使用双 MPU6050 IMU 实现旋转圆盘的闭环角度控制，但因长距离 I2C 信号衰减和电机 EMI 干扰无法可靠读取数据。当前采用纯开环步数计数方案（2230 steps = 360°）。
- **影响**：无法检测步进电机丢失同步（如齿轮卡顿导致的丢步），累积误差可能影响长时间运行的定位精度。
- **可能的改进**：使用差分信号编码器（RS-485）替代 I2C 传感器；或在前段安装独立 MCU 就近读取 IMU 后通过 UART 回传。

### 5.2 扫描臂无位置反馈

- **问题**：扫描臂（Motor 0）未安装限位开关或接近传感器，没有硬件原点。
- **当前做法**：手动遥控 Motor 0 伸出直到探头接触管壁来确定径向零位，无法实现完全自动化的 "伸出→采集→收回" 流程。
- **影响**：自动化序列中摇臂伸出/收回步骤被注释，当前默认序列仅做旋转扫描而无径向动作。

### 5.3 自动化序列为简化版本

- **问题**：当前运行的默认序列是简化的旋转扫描模式（前进→旋转扫描→归位→重复），完整版（含摇臂伸出+超声波采集等待+收回）在注释中。
- **原因**：探头触壁零位需手动确定（见 5.2），使得全自动摇臂操作不可靠。
- **状态**：通过 PS2 ■ 触发简化序列可演示自动化流程的概念，但尚未达到完全无人值守的检测自动化。

### 5.4 三驱动轮统一控制

- Motor A 同时驱动 3 个轮子，无法实现差速转向。在直管中这不是问题，但弯管场景下需要差速。当前通过模块间的万向节被动适应弯管。

### 5.5 旋转速度不可在线调整

- `DEFAULT_SPEED = 5500 μs`（步进间隔）在编译时固定。如果在检测过程中发现需要调整旋转速度（如探头频率响应需匹配扫描速度），必须修改代码重新烧录。
